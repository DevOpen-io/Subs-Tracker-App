import 'package:flutter/material.dart';

class SecondScreen extends StatelessWidget {
  const SecondScreen({super.key});

  @override
  Widget build(BuildContext contenxt) {
    return Column(children: [Center(child: Text("Second Page"))]);
  }
}
